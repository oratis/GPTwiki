import type { DraftArticle } from './types';
import { devPracticesEn } from './dev-practices.en';

// Batch: Programming & Development（中文原生版）。与 dev-practices.en.ts
// 同题同 topicKey，按中文开发者语境原生撰写。配图共用。

const promptOf = (key: string): string => {
  const hit = devPracticesEn.find((d) => d.topicKey === key)?.image?.prompt;
  if (!hit) throw new Error(`no en image prompt for topicKey: ${key}`);
  return hit;
};

export const devPracticesZh: DraftArticle[] = [
  {
    topicKey: 'rebase-vs-merge',
    title: 'Git rebase 和 merge，到底该用哪个？',
    question: 'git merge 和 git rebase 有什么区别？分别在什么时候用？',
    summary:
      'merge 原样保留历史、生成一个合并提交；rebase 把你的提交"重放"到新基底上，得到干净的线性历史。共享分支用 merge，自己的私有提交在分享前用 rebase。',
    tags: ['编程', 'git', '版本控制', '开发工具'],
    language: 'zh',
    image: {
      prompt: promptOf('rebase-vs-merge'),
      alt: '两轨在岔口汇合 vs 一轨被重新铺成单条直线的对照',
    },
    sources: [
      { title: 'Pro Git 官方书——变基（Rebasing）', url: 'https://git-scm.com/book/en/v2/Git-Branching-Rebasing' },
      { title: 'Atlassian——Merging vs Rebasing', url: 'https://www.atlassian.com/git/tutorials/merging-vs-rebasing' },
    ],
    content: `# Git rebase 和 merge，到底该用哪个？

\`merge\` 和 \`rebase\` 解决同一个问题——把一个分支的工作并入另一个——但它们讲了两个不同的故事。merge 记录历史*本来发生的样子*，连分叉一起留下；rebase 则*改写*历史，让它看起来像你一直在一条直线上工作。没有哪个"更正确"，它们为不同目标优化，而那条著名的铁律，正是从各自的行为里直接推导出来的。

## 两个命令到底做了什么

**merge** 把两个分支系到一起，生成一个有两个父提交的新**合并提交**。你的分支提交原地不动；合并提交把两条时间线接上。历史变成一张诚实展示"它们并行发展、然后汇合"的图。

**rebase** 则拎起你分支上的提交，放到一边，移到目标分支的顶端，再把你的提交**一个个重放**上去。结果是一条直线——仿佛你从最新代码开始工作一样。但那些被重放的提交是*带新 ID 的新提交*，原来的被丢弃了。这是关键细节：rebase 改写历史。

## 取舍

| | merge | rebase |
| --- | --- | --- |
| 历史形状 | 分叉的图，忠于事实 | 干净的直线 |
| 产生新提交吗 | 一个合并提交 | 重写所有被重放的提交 |
| 保留上下文 | 是——显示何时何地分叉 | 否——把故事压平 |
| 冲突处理 | 在合并里解一次 | 重放时可能逐提交解 |
| 共享分支上安全吗 | 安全 | 不安全——改写了别人的基底 |

## 那条防灾铁律

**永远不要 rebase 别人已经有的提交。** 因为 rebase 用新提交替换旧提交，对共享/公开分支变基，就改写了别人据以工作的历史——他们下次拉取时，他们的历史和你的对不上，产生重复提交和痛苦的混乱。黄金法则：*对私有的本地工作用 rebase；任何共享的东西用 merge。*

一个常见且安全的工作流把两者结合：独自开发功能分支时，定期把它**rebase 到最新的 main** 上，以干净的历史保持同步；然后要并入共享 main 时，用 **merge**（通常走 PR）。你既得到整洁的本地历史，又得到诚实、非破坏性的集成。

## 实用建议

- **用 merge**：把完成的分支并入共享分支，以及任何分支已经公开的时候。
- **用 rebase**：把进行中的私有分支更新到新的 main 提交上，以及在评审前整理自己凌乱的本地提交（交互式 rebase）。
- **避免 rebase**：在 \`main\`/共享分支上；不确定别人是否已经有你的提交时就停手。

## 常见问题

**rebase 会删掉我的工作吗？**
不会——它重写提交，但改动被保留（旧提交还在 reflog 里可恢复）。它改变的是提交 ID 和顺序，不是你编辑的内容。

**为什么有些团队禁用合并提交？**
他们偏好完全线性的历史，便于阅读和二分查找，所以要求合并前先 rebase（或"squash 合并"）。这是有真实取舍的风格选择，不是对错问题。

**squash 合并是什么？**
它把一个分支的所有提交压缩成目标分支上的单个提交——整洁的"一个功能一个提交"历史，代价是丢失分支内各提交的粒度。`,
  },
  {
    topicKey: 'rest-vs-graphql',
    title: 'REST 和 GraphQL，API 该用哪种风格？',
    question: 'REST 和 GraphQL API 有什么区别？分别在什么场景用？',
    summary:
      'REST 暴露很多固定端点、各自返回设定好的数据形状；GraphQL 只暴露一个端点，由客户端精确指定要哪些字段。GraphQL 擅长复杂多变的数据需求，REST 则更简单、易缓存、无处不在。',
    tags: ['编程', 'api', 'rest', 'graphql'],
    language: 'zh',
    image: {
      prompt: promptOf('rest-vs-graphql'),
      alt: '众多固定贩售格口 vs 单一柜台按清单精确配货的对照',
    },
    sources: [
      { title: 'GraphQL——官方入门', url: 'https://graphql.org/learn/' },
      { title: 'MDN——HTTP 与 REST 概念总览', url: 'https://developer.mozilla.org/en-US/docs/Web/HTTP' },
    ],
    content: `# REST 和 GraphQL，API 该用哪种风格？

REST 和 GraphQL 是设计"客户端如何向服务器要数据"的两种方式。**REST** 给你很多 URL（端点），每个返回一块固定的数据。**GraphQL** 给你单个端点和一套查询语言，客户端*精确*指定要哪些字段，就拿到不多不少的那些。两者的对照归结于：谁来决定响应的形状——服务器（REST）还是客户端（GraphQL）。

## 实践中的差别

假设你想要一个用户的名字，加上他最近三篇文章的标题。

**用 REST**，你通常先调 \`/users/123\`（返回整个用户对象），再调 \`/users/123/posts\`（返回完整的文章对象）。你拿到的远多于所求（每个用户和文章字段都来了），还发了两次往返。这正是 REST 的经典摩擦：**过度获取**（字段太多）和**获取不足**（要凑出一个视图得多次调用）。

**用 GraphQL**，你给单个端点发一条查询，要 \`user.name\` 和 \`user.posts(last: 3).title\`，在一次响应里收到恰好这些字段。客户端在单个请求里拿到了精确的数据。

## 取舍对照表

| | REST | GraphQL |
| --- | --- | --- |
| 端点 | 很多，按资源划分 | 一个 |
| 响应形状 | 服务器固定 | 客户端选择 |
| 过度/不足获取 | 常见 | 设计上避免 |
| 缓存 | 简单（按 URL 走 HTTP 缓存） | 较难（同一 URL，查询各异） |
| 学习曲线与工具 | 更低、通用 | 更高；需要 schema 和服务端层 |
| 版本管理 | 常用 /v1、/v2 | 演进 schema、弃用字段 |
| 最擅长 | 简单、稳定、可缓存的资源 | 复杂、嵌套、按客户端而变的数据 |

## 什么时候选哪个

**选 REST**：你的数据相对简单、是资源形状的，你想倚靠成熟的 HTTP 缓存和工具，你在做一个许多未知客户端都会调用的公开 API，或者你只想要最低摩擦、最普遍被理解的选项。REST 至今仍是默认选择，自有其道理。

**选 GraphQL**：客户端需要从关联丰富的数据里取很多不同切片（移动 App 为减少请求是经典场景，复杂仪表盘也是），你要把多个后端源聚合在一张图后面，或者前端团队想自助迭代数据需求、不必等新端点。代价是服务端复杂度上升、要维护一份 schema、缓存更难。

## 常见问题

**GraphQL 比 REST "更好"吗？**
不——它优雅地解决了过度/不足获取，却增加了复杂度和缓存挑战。对简单 API，REST 往往是更好的工程选择。让工具匹配数据需求。

**能两者都用吗？**
能，而且很常见——很多系统对简单/公开面用 REST，对复杂的内部/App 数据用 GraphQL，或者在 REST 服务前面包一层 GraphQL 网关。

**GraphQL 会取代数据库吗？**
不——它是客户端和服务器之间的 API 查询层。你的服务器仍从数据库或其他服务取数；GraphQL 只塑造客户端收到什么。`,
  },
  {
    topicKey: 'sql-vs-nosql',
    title: 'SQL 和 NoSQL 数据库怎么选？',
    question: 'SQL 和 NoSQL 数据库有什么区别？我该怎么选？',
    summary:
      'SQL 数据库以固定表结构存储结构化的行，擅长关系查询；NoSQL 用一部分结构换来灵活性和更易的横向扩展。怎么选取决于你的数据形状、一致性需求和规模。',
    tags: ['编程', '数据库', 'sql', 'nosql'],
    language: 'zh',
    image: {
      prompt: promptOf('sql-vs-nosql'),
      alt: '规整互联的单元格网格 vs 灵活松散的异形容器集群',
    },
    sources: [
      { title: 'MongoDB——NoSQL vs SQL 数据库', url: 'https://www.mongodb.com/resources/basics/databases/nosql-explained' },
      { title: 'PostgreSQL——关于（关系型数据库）', url: 'https://www.postgresql.org/about/' },
    ],
    content: `# SQL 和 NoSQL 数据库怎么选？

"SQL vs NoSQL"按数据的组织方式给数据库分类。**SQL（关系型）数据库**——PostgreSQL、MySQL 等——把数据存进有预定义结构的行列表格里，并按关系连接各表。**NoSQL** 是其余一切的统称：文档库、键值库、宽列库、图数据库，它们放松僵硬的表结构以换取灵活和扩展。这个决定不是"新与旧"，而是让数据库匹配你数据的形状和系统的需求。

## 核心区别：结构（schema）

**关系型**数据库一上来就要求结构：你定义表和列类型，每一行都遵守，由数据库强制执行。换来的是强大的查询（跨表 SQL join）、强保证，和几十年的可靠性。代价是僵硬——以后改结构要小心，而且关系模型传统上向*上*扩展（更大的服务器）比向*外*扩展（多台服务器）更自然。

**文档型 NoSQL** 数据库（最常见的一类）存储灵活的、类 JSON 的文档。不同记录可以有不同字段；你能把相关数据嵌进一个文档里。这适合快速演进或不规则的数据，并且从设计之初就为**横向扩展**到多台机器而生。代价：内建保证更少、跨记录查询更弱，以及没有结构把关时数据不一致的风险。

## 按类型看取舍

| 类型 | 存什么 | 强在哪 | 示例用途 |
| --- | --- | --- | --- |
| 关系型（SQL） | 表、行 | 复杂查询、事务、完整性 | 金融、订单、一切关系型数据 |
| 文档型 | 类 JSON 文档 | 灵活结构、嵌套数据 | 商品目录、用户资料、内容 |
| 键值型 | 简单的键 → 值 | 极快的查找、缓存 | 会话、缓存、功能开关 |
| 宽列型 | 列可动态变化的行 | 海量写入扩展 | 时序、大规模日志 |
| 图型 | 节点与关系 | 高度关联的数据 | 社交图谱、推荐 |

## 怎么选

问三个问题：

- **你的数据是关系型且一致性至关重要吗？**（钱、库存，任何"做一半的更新"绝不可接受的场景。）→ 选 SQL，为它的事务和完整性。
- **你的数据形状不规则或快速变化，或你需要把写入扩展到多台服务器吗？** → 选一种匹配访问模式的 NoSQL。
- **你的查询模式是什么？** 大量临时 join 和报表偏向 SQL；按键的简单查找偏向键值；深度关联的遍历偏向图。

诚实的现代默认：**没有特定理由的话，从一个扎实的关系型数据库（如 PostgreSQL）起步。** 它能覆盖极广的需求，如今支持 JSON 列以获得灵活性，扩展能力也比人们以为的强。当某个具体需求——极端规模、特定访问模式、真正无结构的数据——要求时，再上 NoSQL。

## 常见问题

**NoSQL 比 SQL 快吗？**
并非天生如此——它对特定模式（简单键查找、海量写入）可能更快，对另一些（复杂 join）更慢或更别扭。"快"完全取决于操作。

**SQL 数据库能撑大型系统吗？**
能——靠复制、分区和缓存，关系型数据库支撑着巨型系统。"SQL 撑不住规模"的说法过时了；扩展只是需要更刻意的设计。

**必须只选一个吗？**
不——"多存储混用"很常见：核心记录用关系型，再加一个键值缓存，也许还有搜索或图存储，各取所长。`,
  },
  {
    topicKey: 'https-how-it-works',
    title: 'HTTPS 是怎么保护你的连接的？',
    question: 'HTTPS 到底做了什么？那把小锁怎么保证我的数据安全？',
    summary:
      'HTTPS 把普通网页流量包进加密层，让你和网站之间的任何人都读不到、也改不了，并用证书证明你真的在和正确的服务器对话。它保护隐私和完整性——但不保证网站本身可信。',
    tags: ['编程', '安全', 'https', '网络'],
    language: 'zh',
    image: {
      prompt: promptOf('https-how-it-works'),
      alt: '消息穿过保护管道、对外人被打乱、终点有验证封印的示意图',
    },
    sources: [
      { title: 'MDN——什么是 HTTPS / TLS', url: 'https://developer.mozilla.org/en-US/docs/Glossary/HTTPS' },
      { title: "Let's Encrypt——它如何工作", url: 'https://letsencrypt.org/how-it-works/' },
    ],
    content: `# HTTPS 是怎么保护你的连接的？

HTTPS 就是 HTTP——网页基本的请求/响应协议——外面包了一层安全（那个 "S" 是 Secure，由 TLS 提供）。纯 HTTP 把一切以可读文本发送，网络路径上的任何人都能看到和篡改。HTTPS 用两个保证修复它：**加密**（外人读不到你的流量）和**身份认证**（你确实在和你以为的那个网站对话，而非冒名顶替者）。那把小锁是关于*连接*的承诺，而搞清它到底承诺什么、不承诺什么，是真正有用的。

## 它解决的两个问题

**1. 窃听。** 在纯 HTTP 上，你的 WiFi 网络、你的运营商、或中间任何人，都能读到你发的每个页面和密码。HTTPS 加密流量，让它对除两个端点外的任何人都是不可读的乱码——尽管它走的是同一条公共互联网。

**2. 冒充与篡改。** 你怎么知道应答的服务器真是你的银行，而不是中途拦截连接的攻击者？HTTPS 用受信任机构签发的**证书**证明服务器身份，并用完整性校验让传输途中的任何篡改都被发现。没有这一层，单有加密也没用——你可能正和一个小偷"私密"聊天。

## 握手怎么进行（简化版）

当你通过 HTTPS 连接时，真正的数据流动之前会有一次快速协商：

1. **证书核验。** 服务器出示证书。你的浏览器核实它由受信任机构为这个确切域名签发、且未过期未吊销——确认身份。
2. **密钥交换。** 用巧妙的密码学（公钥数学），双方在*从不公开发送*的前提下商定一个共享密钥，哪怕有人全程在看。
3. **加密会话。** 此后所有流量都用那个共享密钥加密——会话余下部分用快速的对称加密。

精妙之处在第 2 步：双方在公开信道上建立了一个私密密钥，于是即便窃听者看了整场握手，也算不出它。

## 小锁意味着什么、不意味着什么

| 小锁保证 | 小锁**不**保证 |
| --- | --- |
| 流量在传输中被加密 | 网站诚实或安全 |
| 你连到的是真实域名 | 网站不会骗你 |
| 数据途中未被篡改 | 背后的公司可信 |

这是最被误解的一点：HTTPS 保护的是*管道*，不是*目的地*。钓鱼网站也能有合法的小锁——那只表示你*到骗局*的连接是私密的。HTTPS 保护你的数据不被第三方染指；它不为网站的意图背书。

## 常见问题

**HTTPS 比 HTTP 慢吗？**
如今微乎其微——现代硬件和协议让加密开销极小，HTTPS 还常启用更快的协议特性。"加密很慢"的旧担忧已过时。

**为什么现在连博客都要求 HTTPS？**
因为即便读一个页面也会暴露隐私，而未加密的页面会在传输中被篡改（注入广告/恶意代码）。浏览器现在把纯 HTTP 标为"不安全"，而免费证书又拆掉了成本门槛。

**HTTPS 保护数据到达服务器之后吗？**
不——它保护*传输中*的数据。数据一旦到达服务器，其安全取决于网站如何存储和处理。HTTPS 是一层，不是安全的全部。`,
  },
  {
    topicKey: 'what-is-an-api',
    title: '到底什么是 API？大白话讲清楚',
    question: 'API 到底是什么？用大白话说它怎么工作？',
    summary:
      'API 是一份契约，让一个程序向另一个请求服务，而无需了解对方内部——就像你和后厨之间的那张菜单。它规定你能点什么、会得到什么，把背后的复杂藏起来。',
    tags: ['编程', 'api', '基础概念', '网络'],
    language: 'zh',
    image: {
      prompt: promptOf('what-is-an-api'),
      alt: '隔着柜台递出菜单、藏起后厨、端回成品菜的示意图',
    },
    sources: [
      { title: 'MDN——Web API 入门', url: 'https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Client-side_web_APIs/Introduction' },
      { title: 'Red Hat——什么是 API', url: 'https://www.redhat.com/en/topics/api/what-are-application-programming-interfaces' },
    ],
    content: `# 到底什么是 API？大白话讲清楚

API 的全称是"应用程序编程接口"，这话什么也没解释。这是有用的版本：API 是一份**让两段软件互相对话的契约**——一个程序提供服务，API 规定另一个程序如何请求它们。经典类比是餐厅。你（一个程序）看**菜单**（API），点一道菜（发请求），**后厨**（另一个程序）做好后端出来（响应）。你从不进厨房，也不学它的菜谱；菜单就是你们之间约定的接口。

## 为什么这很重要

那个餐厅安排抓住了全部要点：**你拿到所需，而不必知道它怎么做的。** App 给你显示地图时，它并不包含全世界的地理——它向地图 API 请求。网站让你"用 PayPal 付款"时，它并不自己处理支付——它调 PayPal 的 API。API 让软件建立在软件之上，于是没人需要从零重造地图、支付、天气数据或登录系统。

这带来三大好处：

- **抽象**——你使用服务而不必懂它的内部（点菜不必懂厨房怎么运作）。
- **复用**——一个建好的服务驱动成千上万个 App。
- **解耦**——厨房可以彻底改菜谱，只要菜单不变，你的点单照样有效。团队可以改自己的内部，而不破坏依赖它的所有人。

## 一次 Web API 调用怎么进行

今天最常见的 API 是通过互联网交流的 **Web API**。流程很简单：

1. 你的程序向一个特定 URL（"菜单项"）发**请求**，常带参数（"一杯中杯咖啡，不加糖"）。
2. 服务器收到，干活（查数据库、跑逻辑），然后
3. 发回一个**响应**——通常是 **JSON** 格式的结构化数据，程序很容易读。

例如，天气 App 请求 \`api.weather.com/forecast?city=Tokyo\`，收到带温度和天气的 JSON，再漂亮地显示出来。App 提供问题，API 提供数据。

## API 无处不在

| 你看到 | 背后是一次 API 调用到 |
| --- | --- |
| "用 Google 登录" | Google 的身份认证 API |
| App 里嵌的地图 | 一个地图 API |
| 实时物流追踪 | 快递公司的 API |
| "刷卡支付" | 支付处理商的 API |
| App 里的聊天机器人 | 一个 AI 提供商的 API |

现代软件在很大程度上就是 **API 调用 API**——每个 App 都是一个小厨房，同时也向别人点菜。

## 常见问题

**API 和网站是一回事吗？**
不——网站返回为人排版的页面；API 返回为程序结构化的数据。同样的思路（请求 → 响应），不同的受众。

**API 要花钱吗？**
有些免费，许多按用量收费（按请求或按量），有些需要 API key 来识别和计费。地图、支付和 AI 的 API 普遍按用量计费。

**"API key"是什么？**
一个把你的 App 标识给 API 的秘密令牌，用来认证你、限流、追踪用量——像一张写着"谁在点单"的会员卡。`,
  },
  {
    topicKey: 'sync-vs-async',
    title: '同步与异步：代码为什么等待（或不等待）',
    question: '同步编程和异步编程有什么区别？',
    summary:
      '同步代码一次只做一件事，每步做完才往下走；异步代码可以启动一个慢任务然后先去做别的，结果好了再回来处理。异步让程序在等待网络、文件这类慢操作时保持响应。',
    tags: ['编程', '异步', '并发', '基础概念'],
    language: 'zh',
    image: {
      prompt: promptOf('sync-vs-async'),
      alt: '呆等一口锅 vs 同时照看多口锅的两个厨房对照',
    },
    sources: [
      { title: 'MDN——异步 JavaScript 入门', url: 'https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Asynchronous/Introducing' },
    ],
    content: `# 同步与异步：代码为什么等待（或不等待）

这个区别讲的是**程序在等待时做什么**。**同步**代码一步步运行，每一步必须*彻底完成*下一步才开始——某步一慢，后面全在等。**异步**代码可以*启动*一个慢任务、把它放一边、继续做别的有用的活，结果好了再处理。这个区别对快操作几乎无所谓，对慢操作影响巨大——网络调用、文件读取、数据库查询——而那恰恰是程序大部分空等时间所在。

## 厨房类比

想象做早餐。一个**同步**厨师把面包放进烤面包机，然后*站在那儿盯着*直到它弹出，期间什么都不干，才去煮咖啡。总时间：每一步首尾相接之和，包括所有等待。

一个**异步**厨师启动烤面包，*趁它在烤*，去煮咖啡，*趁咖啡在冲*，去打鸡蛋——哪样好了照看哪样。同样的任务，总时间少得多，因为等待和有用的活重叠了。厨师没变快，只是不再呆站。

## 为什么对真实程序重要

电脑花大量时间等慢东西：服务器应答、磁盘读取、数据库回答——相比处理器的速度，每一个都是漫长。在这些等待中"阻塞"的同步代码，会冻住后面的一切。在 Web 服务器里，一个阻塞调用可能让它在一个慢请求完成前无法服务任何其他用户；在 App 里，就是那个可怕的卡死、无响应的界面。

| | 同步 | 异步 |
| --- | --- | --- |
| 顺序 | 严格一次一个 | 等待期可重叠 |
| 遇到慢步骤 | 一切都等（阻塞） | 别的活继续 |
| 简洁度 | 更易读、易推理 | 控制流更复杂 |
| 最适合 | 快速、有依赖的步骤 | 慢 I/O：网络、文件、数据库 |

## 异步在代码里怎么表达

你不必手动玩杂耍——语言提供了工具。常见模式有**回调**（做完后运行这个）、**Promise/Future**（一个即将到来的结果的占位符），以及现代的 **async/await** 语法，让异步代码*读起来*几乎像同步代码，却保持非阻塞。关键心智模型：\`await\` 意思是"在这里暂停*这个*任务直到结果就绪，但让其他任务同时运行"——而不是"冻结整个程序"。

取舍是真实的：异步对响应性更强大，却更难推理（顺序、错误处理、共享状态都更棘手）。手艺在于把它用在真正发生等待的地方，让简单、快速的逻辑保持同步。

## 常见问题

**异步等于多线程/并行吗？**
不一定。异步关乎不*空等*；它可以在单线程上靠在等待间隙穿插任务来实现。并行是真的在多核上*同时*做事。两者相关但不同——异步重叠等待，并行重叠工作。

**异步会让我的代码跑得更快吗？**
它不加速工作本身；它停止在等待上浪费时间，提升吞吐和响应性。对没有等待的 CPU 密集型工作，单靠异步帮助不大——那时要靠并行。

**为什么异步代码看起来confusing那么多？**
因为执行不再随时间从上到下流动——东西现在启动、稍后完成，错误乱序到来，你既要推理"做什么"也要推理"何时"。async/await 驯服了大部分，但底层的非线性才是真正的难点。`,
  },
  {
    topicKey: 'what-is-docker',
    title: 'Docker 是什么？开发者为什么爱用容器？',
    question: 'Docker 是什么？容器是什么？为什么这么流行？',
    summary:
      '容器把一个应用连同它运行所需的一切打包成一个可移植单元，于是它在哪儿都表现一致——解决了"在我机器上明明能跑"。容器比虚拟机轻，因为它共享宿主操作系统的内核。',
    tags: ['编程', 'docker', '容器', 'devops'],
    language: 'zh',
    image: {
      prompt: promptOf('what-is-docker'),
      alt: '一排封装好的集装箱各装一个自足应用、可互换装上任意平台',
    },
    sources: [
      { title: 'Docker——什么是容器', url: 'https://www.docker.com/resources/what-container/' },
    ],
    content: `# Docker 是什么？开发者为什么爱用容器？

Docker 是让**容器**走向主流的工具，而容器解决了软件最顽固的头疼之一：*"在我机器上明明能跑"*——代码在开发者那儿好好的，到别处却崩，因为另一台电脑版本、设置不同或缺东西。容器的修复方式是**把应用连同它运行所需的一切打包**——代码、运行时、库、系统工具、配置——成一个密封、可移植的单元，在哪儿运行都表现一致。集装箱的类比很贴切：把箱子标准化，任何船、吊车、卡车都能处理它，不必管里面装的是什么。

## 它解决的问题

软件依赖它的环境：特定的语言版本、特定的库、某些系统设置。把应用搬到同事的笔记本、测试服务器或生产环境，任何不匹配都可能让它崩。靠手工在各处复现完全相同的环境既脆弱又抓狂。容器把环境*连同*应用一起打包，于是"环境"随行，没什么可不匹配的。构建一次；它在你的笔记本、队友的机器和云上都跑得一样。

## 容器 vs 虚拟机

容器常被拿来和同样能隔离软件的虚拟机（VM）比较——但分量上的差别才是关键：

| | 虚拟机 | 容器 |
| --- | --- | --- |
| 打包 | 一整个客户操作系统 + 应用 | 只有应用 + 它的依赖 |
| 共享 | 什么都不共享——各带完整 OS | 宿主的 OS 内核 |
| 体积 | 数 GB | 数 MB |
| 启动 | 几分钟 | 几秒甚至更快 |
| 密度 | 一台机器装几个 | 一台机器装很多 |

VM 虚拟化的是一整台电脑，每个应用都背着一个完整操作系统——强大但笨重。容器共享宿主的 OS 内核，只隔离其上的部分，于是显著更轻、启动更快。在只能装几个 VM 的地方，你能跑很多容器。

## 为什么开发和运维都爱它

- **一致性**——消除 dev、test、生产之间的环境漂移。"在我机器上能跑"的借口消亡。
- **可移植**——同一个容器镜像在任何装了容器运行时的机器上都能跑，包括所有主流云。
- **隔离**——每个容器自足，于是依赖冲突的应用能在一台宿主上和平共处。
- **速度与密度**——轻量启动让它非常适合弹性伸缩和高效塞进服务器。
- **现代基础设施的基石**——容器是微服务和编排系统（如 Kubernetes）大规模运行它们的构建块。

## 常见问题

**Docker 和容器是一回事吗？**
不完全——容器是概念/技术；Docker 是构建和运行它们的流行工具集。还有别的工具，但 Docker 普及了这套工作流和镜像格式。

**容器会取代虚拟机吗？**
经常，但并非总是——它们常被*一起*使用（云上容器跑在 VM 里）。VM 在更强隔离和运行不同操作系统上仍重要；容器赢在轻量和速度。

**容器算一道安全边界吗？**
它提供隔离，但比 VM 弱，因为容器共享宿主内核。对多数用途足够；对敌意的多租户负载，团队会加额外加固或与 VM 结合。`,
  },
  {
    topicKey: 'big-o-notation',
    title: '大 O 表示法：不头疼的版本',
    question: '大 O 表示法（Big-O）是什么？程序员为什么在乎它？',
    summary:
      '大 O 描述算法的工作量如何随输入增长而增长——不是确切速度，而是扩展行为。它解释了为什么一种做法在百万级数据上依旧飞快、另一种却卡死，并指导你选对方法。',
    tags: ['编程', '算法', '计算机科学', '性能'],
    language: 'zh',
    image: {
      prompt: promptOf('big-o-notation'),
      alt: '从同一原点发散、从平缓到近乎垂直的多条增长曲线',
    },
    sources: [
      { title: 'Khan Academy——渐进表示法', url: 'https://www.khanacademy.org/computing/computer-science/algorithms/asymptotic-notation/a/asymptotic-notation' },
    ],
    content: `# 大 O 表示法：不头疼的版本

大 O 表示法听着像吓人的数学，但思路简单又实用：它描述**算法做的工作量如何随输入变大而增长。** 它刻意忽略确切时间（那取决于硬件），专注于增长的*形状*。这才是规模化时真正要紧的：一个在 100 项上没问题的算法，到 1000 万项上可能是几分之一秒、也可能是一周，而大 O 在你以痛苦的方式发现之前就告诉你是哪种。

## 为什么"怎么增长"胜过"多快"

真实运行时间取决于机器、语言、当天状态。大 O 把这些剥掉，去比较*扩展行为*——因为那才是经得起增长的。一个在小输入上快一倍、但增长更差的做法，会随数据增长惨败。大 O 回答的不是"它要多久？"而是"**输入变大 10 倍或 1000 倍时会怎样？**"——这才是决定你的软件明年还能不能用的问题。

## 常见的几类，大白话版

把 \`n\` 想成输入的规模（项数）：

| 大 O | 名称 | 大白话 | 例子 |
| --- | --- | --- | --- |
| O(1) | 常数 | 不管多大都同样的工作 | 按下标取一项 |
| O(log n) | 对数 | 增长极慢；每步减半 | 有序数据里的二分查找 |
| O(n) | 线性 | 工作随输入同步增长 | 扫一遍列表 |
| O(n log n) | 线性对数 | 比线性略差 | 优秀的排序算法 |
| O(n²) | 平方 | 工作爆炸；每项对每项 | 两两比较（朴素做法） |
| O(2ⁿ) | 指数 | 灾难性；每加一项翻倍 | 暴力枚举所有组合 |

规模化时鸿沟惊人。对一百万项，O(n) 算法做约一百万步；O(n²) 做约一万亿步——这是"瞬间"和"约等于永远"的差别。这就是为什么藏在循环里的 O(n²) 是经典性能灾难，也是为什么找到 O(n log n) 或 O(n) 的替代能把一个没救的程序变快。

## 实践中怎么用

你不必推证明。实用技能是**识别模式**：对数据的单层循环通常是 O(n)；对同一数据的循环套循环往往是 O(n²)——一个该重新考虑的红旗；反复把问题减半暗示 O(log n)。当某东西在大输入上慢，大 O 思维指向元凶（常是不小心的嵌套循环或慢查找）并指向修法（更好的数据结构或算法）。这也是为什么选对数据结构——用哈希表实现 O(1) 查找、而非扫一遍列表——是杠杆最高的性能决策之一。

## 常见问题

**大 O 越低就一定越快吗？**
对小输入不一定——大 O 描述增长、忽略常数，所以"更差"的类在极小数据上可能赢。它在输入变大时最要紧；对寥寥几项，简单常胜过理论最优。

**最好、平均、最坏情形有什么区别？**
算法可能因输入而表现不同（如已排序 vs 随机）。大 O 常引用最坏情形作为保证，但实践中平均情形往往更要紧。

**大 O 只关乎速度吗？**
不——它也描述**内存**增长（空间复杂度）。一个算法可能快，但内存随输入恶劣增长；选方法时两个维度都重要。`,
  },
  {
    topicKey: 'what-is-caching',
    title: '缓存是什么？为什么它无处不在？',
    question: '缓存（caching）是什么？它怎么工作？为什么对性能这么重要？',
    summary:
      '缓存把数据的副本存在更快够到的地方，于是重复请求能跳过慢速的原始来源。它是计算中最强大的提速技巧之一——每一层都在用——而它最难的问题是判断副本何时过期。',
    tags: ['编程', '缓存', '性能', '系统'],
    language: 'zh',
    image: {
      prompt: promptOf('what-is-caching'),
      alt: '常用物件放在近处小架（短路径）vs 存在远处仓库（长路径）的对照',
    },
    sources: [
      { title: 'MDN——HTTP 缓存', url: 'https://developer.mozilla.org/en-US/docs/Web/HTTP/Caching' },
      { title: 'Cloudflare——什么是缓存', url: 'https://www.cloudflare.com/learning/cdn/what-is-caching/' },
    ],
    content: `# 缓存是什么？为什么它无处不在？

缓存是一份存在某个快地方的副本，于是你不必每次都从某个慢地方取。原理很直觉：如果你老是要拿同一本书，你把它放在桌上，而不是每次走去图书馆。在计算里，"图书馆"可能是数据库、远处的服务器或慢速磁盘，"你的桌子"是近旁更快的内存。缓存是**整个计算领域最通用的性能技术之一**——几乎每一层都有——因为快慢存储之间的速度差距巨大，而大多数系统一遍遍地请求同样的东西。

## 为什么它这么有效

两个事实让缓存收益巨大。第一，**速度差距很大**：从内存读取可能比从磁盘或跨网络快几千倍。第二，**访问是重复的**：程序和用户倾向于反复要同样的数据（热门视频、登录用户的资料、首页）。把常要的东西放近，绝大多数请求就被快速服务，只偶尔付慢路径的代价。哪怕只缓存一小撮"热"数据，也能服务大部分流量。

## 缓存在每一层

你被无形工作的缓存包围着：

| 缓存 | 它加速什么 |
| --- | --- |
| CPU 缓存 | 处理器比主存更快地够到数据 |
| 浏览器缓存 | 重新打开网站时不必重下图片/脚本 |
| CDN（内容分发网络） | 从离你近的服务器供内容，而非跨越半个地球 |
| 应用/内存缓存（如 Redis） | 避免重复的数据库查询 |
| 数据库缓存 | 复用近期查询的结果 |
| DNS 缓存 | 跳过重复的地址查询 |

单次页面加载可能受益于叠在一起的半打缓存——这就是为什么第二次访问一个网站比第一次快得多。

## 难点：判断副本何时过期

缓存著名的难处不在存副本，而在判断**副本何时过时。** 如果原始数据变了而缓存仍供旧副本，用户就看到错误信息（已经改过的价格、已被更新的资料）。这叫"缓存失效"，是那个关于计算机科学两大难题的著名玩笑的一半。系统用这些策略管理它：**过期**（副本活一段设定时间后刷新）、**失效**（源变化时主动清掉副本），以及接受**最终一致性**（为速度容忍短暂的过时）。决定数据必须多新——相对它必须多快——是缓存的核心取舍。

## 常见问题

**为什么不干脆永久缓存一切？**
因为数据会变，过时副本导致 bug；而且缓存空间有限，会淘汰不常用的项。缓存用完美的新鲜换速度——你缓存那些"略旧也安全"的东西。

**"清除缓存"修的是什么？**
它强制从源头取新副本。当网站看起来坏了或过时，你的浏览器可能在显示过时的缓存文件；清除让它重新取当前版本。

**缓存会导致 bug 吗？**
会——供过时数据是经典的那种。出人意料的一大批"它怎么不更新！"问题，是链路某处的缓存攥着旧副本。强大，但确实是微妙问题的来源。`,
  },
];
